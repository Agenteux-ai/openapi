{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "type" : "object",
  "properties" : {
    "content" : {
      "type" : "string",
      "maxLength" : 102400,
	  "description" : "The prompt template content with placeholders for variables. Variables are of the form: {{variable_name[:type][=default_value]}}"
    },
    "role" : {
      "type" : "string",
      "enum" : [ "system", "user", "assistant" ],
	  "description" : "The role of this message within the prompt template, which can be 'system', 'user', or 'assistant'."
    }
  },
  "required" : [ "content", "role" ],
  "$id" : "https://schema.agenteux.com/prompt/Message.json",
  "title" : "Message",
  "description" : "JSON Schema definition for an LLM Message, multiple of which make up a prompt template"
}